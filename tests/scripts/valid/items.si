struct Position {
    x: f32,
    y: f32,
}

enum Direction {
    Left,
    Right,
}

const SPEED: f32 = 1.0;
type Index = u32;

extern fn draw(pos: &Position);

export fn update(pos: &mut Position) {
    let dx: f32 = SPEED;
}
